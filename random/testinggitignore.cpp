#include<iostream>
#include<string>
#include<vector>
#include<unordered_map>
#include<map>
using namespace std;
#define ll long long
int main()
{
	ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    cout<<"Bismillah"<<endl;
    cout<<"After adding .exe in .gitignore file. The .exe files will no longer be added in git status"<<endl;

    return 0;
}